# Python
.venv/
__pycache__/
*.pyc
*.db

# Node / Bun (bun.lock stays committed)
node_modules/
dist/

# PyInstaller desktop build (`dist/` above already covers backend/dist)
backend/build/

# Environment
.env

# Database dumps written by the docker-compose backup sidecar — personal
# data, never commit these.
backups/

# Misc
.DS_Store
*.log
